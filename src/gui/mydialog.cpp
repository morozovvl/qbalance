#include "mydialog.h"

MyDialog::MyDialog(QWidget *parent) : QDialog(parent) {
    setObjectName("MyDialog");
}

/*
QString MyDialog::windowTitle() {
//    return QDialog::windowTitle();
    return "windowTitle";
}
*/
